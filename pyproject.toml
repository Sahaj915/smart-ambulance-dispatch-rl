[project]
name = "smart-ambulance-dispatch-rl"
version = "0.1.0"
description = "Smart ambulance dispatch"
requires-python = ">=3.11"
dependencies = [
    "openenv-core>=0.2.0",
    "fastapi",
    "uvicorn",
]

[project.scripts]
server = "server.app:main"